# Example Infrastructure

This folder is reserved for optional example deployments.

The default Irongate deploy does not create frontend hosting, sample apps, or
sample resource APIs. Future example resources should be enabled explicitly from
stage config before their modules create AWS or third-party resources.
